---
lesson: "python-05-08"
track: "python"
decision: "本课的学习障碍集中在异常传播、控制流与资源清理。读完文字后仍需同时追踪“ExitStack：动态资源、部分获取与所有权转移”中的多项变化，因此用顺序流程把相邻正文的真实机制拆成可单步核对的状态。"
---

## 视觉实验

### 让输入穿过“ExitStack：动态资源、部分获取与所有权转移”

id: "python-05-08-main"
kind: "flow"
placement: "mechanisms"
summary: "把本课核心机制中的真实状态与约束按步骤展开，使学习者能观察“ExitStack：动态资源、部分获取与所有权转移”中先发生什么、后发生什么，以及哪一个不变量不能被视觉过渡掩盖。"
caption: "当前高亮表示输入沿处理阶段的推进次序；步骤文字直接取自本课核心机制。动画不表示 CPython 的真实耗时、内存比例或跨版本私有布局，最终以官方入口、源码节选与可运行断言为证据。"
actionLabel: "播放处理流程"

#### 步骤

- callback(fn | callback(fn, *args) 只做无异常三元组的清理，不能抑制异常；push(exit) 可参与抑制。
- 每个 exit 看到前一个内层 ex… | 每个 exit 看到前一个内层 exit 处理后的最新异常。
- close 等价于以正常退出三元组立… | close 等价于以正常退出三元组立即展开 stack。
- pop_all 返回新 stack | pop_all 返回新 stack，原 stack 变空；遗失新 stack 会造成资源责任泄漏。

#### 观察重点

- 推进前先预测下一步会改变异常传播、控制流与资源清理中的哪一项，并指出至少一个必须保持不变的条件。
- 视觉省略了平台、实现和版本分支；涉及 CPython 私有细节时，回到本课官方入口、源码路径与运行示例复核。
