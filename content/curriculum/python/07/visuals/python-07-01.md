---
lesson: "python-07-01"
track: "python"
decision: "三种物理布局同时比较时，格子图能让搬移、两端和探测差异可见。"
---

## 视觉实验

### 容器布局

id: "python-07-01-mechanism"
kind: "tensor"
placement: "chapter:2"
summary: "把操作映射到数组、块链和哈希槽 视觉中的每个阶段都能回到正文、源码和断言验证，适用于本课而不是装饰性动画。"
caption: "视觉只表达 Python 3.14 / CPython v3.14.6 的教学模型；精确语义和性能结论回到官方章节、固定源码与示例断言。"
actionLabel: "推进容器布局"

#### 步骤

- 输入 | list 尾部追加利用连续数组和过度分配，deque 两端操作避免整体搬移，dict/set 用哈希槽提供平均常数时间。 的最小输入和初始状态。
- 模型 | 复杂度要同时看操作序列、布局和数据分布；选择依据来自热点负载而非单一 Big-O。 中的当前不变量和所有权。
- 主路径 | list_resize 增长并复制指针，deque 维护块链，dict/set 探测槽位；缓存局部性和碰撞改变常数。 对照源码入口推进一次。
- 失败 | 头部 pop、不可哈希元素进 set、把平均 O(1) 当最坏保证，都会让负载退化。 注入失败并记录传播或回滚。
- 边界 | 顺序索引用 list，两端队列用 deque，唯一性用 set，关联用 dict；热点用基准验证。 识别工程和版本取舍。

#### 观察重点

- 下一步前预测哪个量会改变、哪个不变量必须保持，再用示例正常断言核对。
- 指出视觉简化可能失真的平台、并发、资源或版本边界，回到源码和官方文档验证。
