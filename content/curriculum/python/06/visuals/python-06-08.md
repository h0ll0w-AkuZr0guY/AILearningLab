---
lesson: "python-06-08"
track: "python"
decision: "本课的学习障碍集中在导入查找、模块缓存与构建管线。读完文字后仍需同时追踪“循环导入、半初始化模块与依赖方向”中的多项变化，因此用关系图把相邻正文的真实机制拆成可单步核对的状态。"
---

## 视觉实验

### 沿关系逐点追踪“循环导入、半初始化模块与依赖方向”

id: "python-06-08-main"
kind: "graph"
placement: "mechanisms"
summary: "把本课核心机制中的真实状态与约束按步骤展开，使学习者能观察“循环导入、半初始化模块与依赖方向”中先发生什么、后发生什么，以及哪一个不变量不能被视觉过渡掩盖。"
caption: "当前高亮表示对象、名称、类型或依赖之间的关系；步骤文字直接取自本课核心机制。动画不表示 CPython 的真实耗时、内存比例或跨版本私有布局，最终以官方入口、源码节选与可运行断言为证据。"
actionLabel: "推进关系追踪"

#### 步骤

- 同线程递归导入依赖预插入避免死循环 | 同线程递归导入依赖预插入避免死循环。
- 父包 spec 记录 uniniti… | 父包 spec 记录 uninitialized_submodules，改善部分初始化诊断。
- 模块顶层定义顺序影响环中可见属性 | 模块顶层定义顺序影响环中可见属性。
- 类型注解可用 TYPE_CHECKI… | 类型注解可用 TYPE_CHECKING、前向引用和延迟求值减少运行时环。

#### 观察重点

- 推进前先预测下一步会改变导入查找、模块缓存与构建管线中的哪一项，并指出至少一个必须保持不变的条件。
- 视觉省略了平台、实现和版本分支；涉及 CPython 私有细节时，回到本课官方入口、源码路径与运行示例复核。
